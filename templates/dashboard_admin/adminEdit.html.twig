{% extends 'base.html.twig' %}
{% block title %}Modification - Admin{% endblock %}

{% block body %}
<h1 class="titre">Modifier ce profil</h1>


<div class="form-block">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="divBtn">
            <button type="submit" class= 'btn btn-warning'>Modifier</button>
            <a href="{{ path('app_dashboard_admin')}}" class= 'btn btn-primary'>retour</a>
        </div>
    {{ form_end(form) }}

<button type="button" class="btn btn-secondary" onclick="window.history.back();">r</button>
{# <a href="{{ app.request.headers.get('referer') }}" class="btn btn-secondary">Retour</a> #}
</div>
{% endblock %}






